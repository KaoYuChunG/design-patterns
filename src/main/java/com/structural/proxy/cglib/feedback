1. 屬於動態代理。
2. 是一個強大高性能的代碼生成包，在運行期擴展java類與實現java接口，被廣泛使用在AOP框架，如spring AOP，實現方法攔截。
3. AOP編程中如何使用代理模式，要看其要求。如目標對象要實現接口，那要使用JDK。如不用，那可以使用cglib。
4. cglib 是透過字節碼處理框架ASM來轉換字節碼並生成新的類。
5. 被代理的類不可以是final類。
6. 目標對象中的方法如果是final或static就不會被攔截，也不會執行目標對象的額外的業務方法。



